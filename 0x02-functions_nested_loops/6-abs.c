#include "main.h"

/**
 * _abs -> prints the absolute value of an integer n
 * @n: takes in an integer n
 *
 * Return: absolute value of an integer
 */
int _abs(int)
{
	if (n < 0)
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
		return (0);
	}
}

